<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles-celebrity-form.css">
    <title>Document</title>
</head>
<body>
    <div class="celebrity-form-container">
        <div class="name-input-container">
            <div class="label"><label for="name">Enter celebrity name: </label></div>
            <div class="input"><input type="text" name="name" id="name" value="<%= locals.celebrity?.name || '' %>" required/></div>
        </div>
        <div class="birthdate-input-container">
        <div class="label"><label for="birthdate">Enter celebrity birthdate: </label></div>
        <div class="input"><input type="date" name="birthdate" id="birthdate" value="<%= locals.celebrity?.birthdate || '' %>" required/></div>
        </div>
        <div class="category-input-container">
        <div class="label"><label for="category-select">Select celebrity category </label></div>
        <div class="input">
            <select name="categoryId" id="categoryId" >
                <% if (locals.categories) { %>
                        <% for( const category of categories) { %>
                                <option value="<%= category?.id || '' %>"  <% if(locals.celebrity?.categoryId === category.id){%> selected <%}%> ><%= category?.name || '' %></option>
                        <% } %>
                <% } %>
            </select>
        </div>
        </div>
        <div class="description-input-container">
        <div class="label"><label for="description">Enter celebrity bio: </label></div>
        <div class="input"><textarea name="description" id="description" cols="30" rows="10" required> <%= locals.celebrity?.description || '' %></textarea></div>
        </div>
    </div>
</body>
</html>
